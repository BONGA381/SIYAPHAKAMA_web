services:
  - type: web
    name: siyaphakama
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: siyaphakama-db
          property: connectionString
      - key: PYTHON_VERSION
        value: 3.11.0
